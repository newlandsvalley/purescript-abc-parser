{
  "private": true,
  "scripts": {
    "build": "spago build",
    "test": "spago -x ./test.dhall test",
    "pulp-build": "pulp build"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "purescript": "^0.14.0",
    "purescript-psa": "^0.6.0",
    "webpack": "^3.6.0"
  }
}
